<?php

function contact_alter_form_alter(&$form, &$form_state, $form_id) {
    if ($form_id == 'contact_site_form') {
        $form['copy'] = [];
        $form['subject'] = [];
    }
    print_r($form_id);
}
